["# Setup pstack","## Roles","## Steps","### 1. Detect available models","### 2. Load current state","### 3. Map and confirm","### 4. Validate","### 5. Write and regenerate","### 6. Confirm","### 7. Offer a verification skill (optional)","### Autonomous run","### Autopilot-full","### Autopilot-stack","### Orchestrate","**Bounded by the session in this port.** Cursor's `/loop` could re-arm a sleeping agent.","**DEGRADED IN THIS PORT.** Same constraint as **Autopilot-full**. Owners are local awaited","**DEGRADED IN THIS PORT.** This playbook assumes Cursor cloud agents: one long-lived owner","**DEGRADED IN THIS PORT.** Upstream pstack runs workers as Cursor cloud agents: each gets its","**Defaults for every `task` call.** File pointers, not inlined context. One writer per","**Degraded from upstream pstack.** Upstream runs workers as Cursor cloud agents, which have","**Degraded in this port.** Upstream pstack runs fan-out as Cursor cloud agents, each with","**In opencode, model choice is which subagent you pick.** `task` accepts","**Reach for a `-read` variant by default.** It cannot write, and it keeps MCP access. Only","**Use `subagent_type: poteto-agent` for an ad-hoc delegate inside a playbook step** when it","**show-me-your-work** skill so a fresh session can resume from the trail rather than restart.","- **Every worker shares one filesystem.** Worktrees are mandatory, not an optimization.","- **Fan-out costs coordinator wall-clock.** Prefer few broad workers. A hundred-subagent","- **Nesting depth is capped by config.** Upstream relies on depth 3 (coordinator, track,","- **Nesting is shallow.** An owner spawning its own verifier is depth 3, which is unverified","- **No wake chain.** Audit ticks happen when you drive them in this session. Close the","- **One owner at a time, not a queue in flight.** Take PRs sequentially, or give each owner","- **Opening a PR.** Invoked at the end of every other playbook. `playbooks/opening-a-pr.md`.","- **The four role models should be genuinely different**, and ideally from different","- **Worker / verifier.** Always local, always awaited. Every worker shares this machine and this checkout, so the isolation upstream pstack got for free from cloud agents you now have to build: one writer per worktree or branch, no exceptions (**principle-separate-before-serializing-shared-state**). Prefer few, broad workers over many narrow ones, because the coordinator blocks on each fan-out. Workers can read the local store and local transcripts directly, which cloud agents could not, so briefs point at paths instead of inlining content. Run a unit's verifier on a different subagent from its worker, so the model that wrote the code is not the model that clears it.","- **`fast` should actually be fast.** It carries swarm fan-out and mechanical edits, where","- Generated: `~/.config/opencode/agent/pstack-*.md`, `poteto.md`, `comment-sicko.md`","- Manifest: `~/.config/opencode/pstack-sync/models.json`","- Shipping UI / IDE / CLI → drive the real surface yourself. There is no bundled control skill here, so use the project's own `verify-*` skill, or generate one with **create-verification-skill** when none exists. For bug fixes, reproduce first on the same surface yourself; hand to the user only under the narrow Bug fix step 1 exception.","- There is no `cloud_base_branch`. A worker that must start from a non-default branch needs","- Workers share one filesystem. Give each its own output path (worktree, branch, or","---","/create-verification-skill.\" On yes, invoke `/create-verification-skill`. On no, move on","3. Pick the runners. Default to one each on `pstack-judgment`, `pstack-precise`,","3. Set N from the user or derive it from the shape. Workers run locally and are awaited,","4. Workers run on `pstack-fast` by default. Use `pstack-fast-read` when the slice only","After all Phase B candidates complete, pick one judge from `pstack-judgment-read`,","After the explanation is complete, spawn one architectural critic per panel agent, all in","At the end of the run, before handing back, check the log told the truth. Capture this session's transcript with","Auto. `poteto` is the sticky primary agent and takes the chat model you want to live in.","Capture this session's transcript with","Check whether the project has a way to drive the real app for proof (a `verify-*` skill, or","Create the store at `.opencode/orchestrate/<project-slug>/` inside the project, so it travels","Each pins a different model and none can write. Model choice is the subagent you pick, so","Every file has exactly one writer; owners publish facts, readers aggregate at read time. Use","Every model written must be in the detected set. If a chosen one is not available, stop and","Four agents, four different models, none able to write. The diversity is the point. Running","Four roles, four models. Panels spawn one subagent per role, which is where model diversity","Its quality check spot-verifies citations, which needs MCP access. The `-read` agents keep","Launch all four reviewers in a single message, one `task` call each:","MCP access for context lookups (tickets, chat threads, observability traces referenced in the","MCP, so read-only does not defeat that here.","MCP-backed investigator. opencode has no such coupling, so read-only is a real sandbox","Never hand-edit the generated agent files. Every sync overwrites them.","Once all explorers return, spawn a single subagent to synthesize their findings into one","One `task` call with `subagent_type: pstack-judgment-read`. Its quality check spot-verifies","One message, three `task` calls, one per row in the table below. The `-read` agents keep","Read `~/.config/opencode/pstack-sync/models.json`. Its `roles` map is the current","Read that before promising anyone an overnight run.","Run `/setup-pstack` to change which model backs which role.","Run `opencode models`. That is the dependable source. If it fails, ask the user to paste the","Show every role with its current model and purpose. Mark any model not in the detected set as","Show the user the final role table and tell them new sessions pick it up. Confirm the","Spawn a single subagent that explores and explains in one pass, with","Spawn all N subagents in one message, each with the task, the path to the shared","Spawn all N workers in one message, one `task` call each, with the agent chosen in Phase A.","Spawn all explorers in a single message with `subagent_type: pstack-fast-read`.","Spawn each investigator with `subagent_type: pstack-fast-read`.","Spawn exactly one; the `-read` agents cannot write, which is what makes a judge safe.","Spawn one synthesizer subagent with `subagent_type: pstack-judgment-read`.","Subagents run locally against this one working tree and the coordinator blocks until each","That agent cannot write and still has full MCP access. Upstream pstack warns against","That agent pins the fast code model and cannot write. Model choice in opencode is which","That rewrites every `agent/pstack-*.md` from the manifest and runs the validator. A non-zero","The Graphite mechanics and the single-writer-on-topology rule port unchanged, and they are","The agent does its own exploration","The parent captures its own transcript before fanning out. Run","The verdict discipline is what is worth keeping: nothing merges without an independent","Three consequences, and they change how you plan a program.","Two more agents are not role-backed. `poteto-agent` deliberately omits `model` so it runs on","Two properties matter more than any individual pick.","Use fewer than four only when the artifact is genuinely small. Adding a fifth reviewer means","Use one runner per panel agent: `pstack-judgment`, `pstack-precise`, `pstack-fast`,","Walk the log against what actually happened:","What that changes.","Write the chosen values into the `roles` map in `models.json`, preserving the `purpose`","Your job is to edit one manifest and regenerate those agent files.","`--all-projects`; that reads private chats from unrelated projects.","`/tmp/swarm-<slug>/worker-<n>/`) per the **separate-before-serializing-shared-state**","```","`arena`, `architect`) name their own subagents for diverse-model review; respect what the","`description`, `prompt`, `subagent_type`, `task_id` and nothing else. There is no `model`,","`pstack-fast`, `pstack-opus`, which is four different models. Spawn more when the arena","`pstack-opus`. Those four pin four different models, which is where the design diversity","`pstack-precise-read`, `pstack-fast-read`, `pstack-opus-read`. Prefer a different model","`readonly`, `run_in_background`, or `environment` parameter. Every per-call knob upstream","`subagent_type: pstack-judgment-read`.","`~/.opencode/opencode.jsonc`, so depth 3 fits. Verify it before relying on it, and if a","a real `git worktree` on that branch, created by you in Phase A, with its path in the brief.","a single message:","a worker needing help returns a `NEEDS_HELPER` request instead of spawning its own.","an existing harness). If not, offer once: \"want a project-local verification skill, so agents","ask again.","assignment. Treat it as the starting point.","before promising anyone an unattended run.","bun run ~/.config/opencode/skills/poteto-mode/scripts/orch/orch.ts --store .opencode/orchestrate/<project-slug>","bun run ~/.config/opencode/skills/poteto-mode/scripts/session-digest.ts latest --tools","bun run ~/.config/opencode/skills/poteto-mode/scripts/session-digest.ts latest --tools > /tmp/reflect-transcript.md","call, backed by an always-applied rule file. opencode has no such parameter. A subagent's","can drive the app the way a user does and prove changes work? I can generate one with","cd ~/.config/opencode/pstack-sync && bun run sync.ts","change which model backs a role.","citations, which needs MCP access; the `-read` agents keep it. Use `references/synthesizer.md` verbatim","coherent explanation, with `subagent_type: pstack-judgment-read`.","comes from. Each role also has a read-only variant with the same model and `edit: deny`,","comes from. Run `/setup-pstack` to change which model backs a role.","consequences you must design around.","covers multiple design directions. Reuse one agent N times when the work is","description: Configure which models pstack uses per role. Detects your available models and rewrites the pstack subagent definitions. Use for /setup-pstack, \"configure pstack models\", or changing pstack's model choices.","detected models as options. Use the `question` tool rather than free text.","each session's recorded directory, and `list` / `find` show what else is available for this","exit means the generated config is broken; report the failure instead of declaring success.","families. Panels exist to surface disagreement. Four roles on one model gives you one","family from this chat's own model, so `pstack-precise-read` when the chat runs on Anthropic.","fan-out, and nothing re-arms itself between turns.","for bookkeeping, written below as `orch`, while its canonical plain TSV and JSON stay","four critics on one model gives you one opinion four times.","from the project directory, and pass that file path to the reviewers. The script scopes to","frontier logic. What does not survive is cheap unattended concurrency.","generation-bound rather than judgment-sensitive. Run `/setup-pstack` to change which","grounding, its own output path, and instructions to produce both the artifact and a short","has no equivalent: subagents run locally against this working tree and the parent awaits","here. Have the root spawn verifiers itself.","its own `git worktree` and accept that the root waits.","its own checkout, so a coordinator can supervise many long-lived workers cheaply. opencode","latency multiplies by N.","mid-task.","model IDs they have access to. Never write a model ID you have not confirmed exists; an agent","model backs a role.","model is fixed by its agent definition, so the role table lives in agent files instead.","name: setup-pstack","needing a choice. Then ask whether to accept as-is or change specific roles, offering the","none of those. Subagents run locally against this one working tree, the root blocks on every","not told about.","one filesystem, and let the root spawn verifiers rather than nesting them under owners.","ones, since two arms on the same agent race the same model against itself.","opencode cannot: nothing wakes a session, so an autonomous run lasts exactly as long as this","opinion four times, which reads as consensus and is not.","output path. Subagents are awaited, not backgrounded, so scope each one to finish.","own checkout, runs unattended, and is polled rather than awaited. opencode has none of that.","per PR, each in its own checkout, polled on a wake chain while the root sleeps. opencode has","pointing at a missing model fails at spawn time, not at load time, so the breakage surfaces","principle skill. This is now mandatory, not a nicety.","program is not achievable here.","project. Never pass `--all-projects`; that reads private chats from unrelated projects.","pstack picks a model per role. In Cursor that was a `model` parameter on every subagent","pstack reaches for is instead baked into an agent definition.","rather than a posture, and the warning does not apply.","rationale. Candidates write, so use the write-capable agents here, not the `-read` variants.","read-only mode here because Cursor's read-only stripped MCP, which would disable every","readable without the CLI.","reads. For a model race, name each arm's agent up front and pick genuinely different","returns. What survives is the bookkeeping discipline, the brief-is-the-product rule, and the","reusing a model, which buys agreement rather than coverage.","run from the project directory. The script scopes to the current project by filtering on","runs locally against this working tree and the parent waits for all of them. Two","session and the run stops.","session does. Drive the loop yourself in-session, and checkpoint through the","should run on this chat's model and carry this style. Tier code delegates by difficulty using","single digits unless the slices are tiny.","skill prescribes and don't override to `poteto-agent`.","sleeper wake chain for audit ticks. Give every owner its own `git worktree`, since they share","so N is bounded by this machine and your patience, not by a cloud quota. Keep it in the","so pick this path explicitly and put it in every brief; a worker cannot find a store it was","spawn is refused fall back to root-spawns-all: the coordinator makes every `task` call and","strings. Then regenerate:","subagent you pick, not a `task` parameter. Run `/setup-pstack` to change which model backs","subagents, not cloud agents, so the queue runs narrow rather than wide and there is no","the current project by filtering on each session's recorded directory. Never pass","the parent chat model, which is pstack's `inherit-parent` behavior and how Auto users stay on","the part that actually keeps a stack landable.","the role.","the table above. Routed workflow skills (`how`, `why`, `interrogate`, `reflect`, `swarm`,","their own checkout and their own base branch. opencode has no equivalent, so every worker","them. **Orchestrate**, **Autopilot-full**, and **Autopilot-stack** still work, but flattened","there is no slug to get wrong and no `model` parameter to pass. Run `/setup-pstack` to","to root-spawns-all with fewer, broader workers. Read the degradation note at the top of each","transcript) while being unable to write, so the parent applies every edit.","use a write-capable agent when the delegate must actually produce a diff.","used by every review and investigation path.","validator passed. Re-running this skill updates the mapping.","verdict from an agent that did not write the code.","with the repo and survives a restart. Nothing in opencode names a per-agent store directory,","without pushing.","worker). opencode caps this with `subagent_depth`, currently 4 in","| A | `pstack-judgment-read` |","| B | `pstack-precise-read` |","| C | `pstack-fast-read` |","| Critic | Subagent |","| D | `pstack-opus-read` |","| Divergent | `pstack-opus-read` | `references/divergent-reviewer.md` |","| Judgment | `pstack-judgment-read` | `references/judgment-reviewer.md` |","| Reviewer | Subagent |","| Role | Agents | Used for |","| Subagent | Model role | Writes | Use for |","| Tooling | `pstack-precise-read` | `references/tooling-reviewer.md` |","| `comment-sicko` | judgment | no | comment review |","| `fast` | `pstack-fast`, `pstack-fast-read` | mechanical edits, swarm workers, code exploration |","| `judgment` | `pstack-judgment`, `pstack-judgment-read` | hardest changes, prose, synthesis |","| `opus` | `pstack-opus`, `pstack-opus-read` | fourth panel member |","| `poteto-agent` | this chat's model | yes | a delegate that should inherit your own model and this style |","| `precise` | `pstack-precise`, `pstack-precise-read` | precisely specified step sequences |","| `pstack-*-read` | same as above | no | every review, critique, investigation, and exploration path |","| `pstack-fast` | fast | yes | trivial mechanical edits, swarm workers |","| `pstack-judgment` | strongest judgment | yes | the hardest changes: cross-cutting design, gnarly concurrency, subtle algorithms, vague intent |","| `pstack-opus` | fourth panel model | yes | panel member, second opinions |","| `pstack-precise` | strongest instruction-following | yes | a precisely specified sequence of steps to execute to the letter |","|----------|-----------|--------|---------|","|----------|----------|","|--------|----------|","|------|--------|----------|"]
